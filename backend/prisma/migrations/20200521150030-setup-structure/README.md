# Migration `20200521150030-setup-structure`

This migration has been generated by João Felipe at 5/21/2020, 3:00:30 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `playerum`.`users` (
`created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`email` varchar(191) NOT NULL  ,`id` int NOT NULL  AUTO_INCREMENT,`name` varchar(191)   ,`password` varchar(191) NOT NULL  ,`updated_at` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `playerum`.`places` (
`created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` int NOT NULL  AUTO_INCREMENT,`image_url` varchar(191)   ,`name` varchar(191) NOT NULL  ,`updated_at` datetime(3) NOT NULL  ,`votes` int NOT NULL DEFAULT 0 ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `users.email` ON `playerum`.`users`(`email`)

CREATE UNIQUE INDEX `places.name` ON `playerum`.`places`(`name`)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200521150030-setup-structure
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,26 @@
+datasource db {
+  provider = "mysql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model users {
+  id         Int      @default(autoincrement()) @id
+  email      String   @unique
+  password   String
+  name       String?
+  created_at DateTime @default(now())
+  updated_at DateTime @updatedAt
+}
+
+model places {
+  id         Int      @default(autoincrement()) @id
+  name       String   @unique
+  votes      Int      @default(0)
+  image_url  String?
+  created_at DateTime @default(now())
+  updated_at DateTime @updatedAt
+}
```


